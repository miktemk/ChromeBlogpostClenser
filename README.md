# ChromeBlogpostClenser

A chrome extension that removes some elements from the page to make it more text-to-speech friendly

For example codeproject.com/Articles:

  $("pre").hide();
  $("img").hide();
  $(".pre-action-link").hide(); // Show/Hide code
